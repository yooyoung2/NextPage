<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.generation.website.templates.dao.TemplatesDAO">

	<select id = "getFooter" parameterType="string" resultType="TemplatesVO">
		SELECT * FROM GEN_TEMPLATE WHERE TMPLT_ID = #{tmpltId}
	</select>

</mapper>
