<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="..DAO"> <!-- DAO경로 써야함 -->
<!--
	예시 : kr.or.ddit.board.dao.BoardDAO 
 -->

	<!-- 쿼리 예시
	
	<delete id="deleteAttathes" parameterType="int">
		DELETE FROM ATTATCH
		WHERE BO_NO = #{boNo}
	</delete>
	
	<delete id="deleteAttatches" parameterType="int[]">
		DELETE FROM ATTATCH
		WHERE ATT_NO IN 
			<foreach collection="delAttNos" open="(" close=")" item="attNo" separator=",">
				#{attNo}
			</foreach>
		
	</delete>
	<select id="selectAttatch" parameterType="int" resultType="AttatchVO">
		SELECT	ATT_FILENAME, ATT_SAVENAME
			, ATT_FILESIZE, ATT_MIME
		FROM ATTATCH
		WHERE ATT_NO = #{attNo}
	</select>
	 
	 -->
	
</mapper>

















